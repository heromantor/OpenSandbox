# Downloaded kernel artifacts (fetched via `make fetch-kernel`).
# Never commit these; they are reproducible from DefaultKernelURL.
kernel/
coverage.out

# Phase 2: local rootfs cache and downloaded OCI test fixtures
rootfs-cache/
image/testdata/oci-*
